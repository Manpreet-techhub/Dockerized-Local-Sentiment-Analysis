version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    networks:
      - sentimentanalysis
  
  frontend:
    build: ./frontend
    ports:
      - "5000:5000"
    networks:
      - sentimentanalysis

networks:
  sentimentanalysis:
    driver: bridge

